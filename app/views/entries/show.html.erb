<h4 id="entry-name"><%= @entry.name %></h4>
<div class="row">
  <div class="col-xs-3">
    <div class="well">
      <% @entries.each do |entry| %>
        <p>
          <%= link_to entry.name, collection_entry_path(@collection, entry) %>
        </p>
      <% end %>
    </div>
  </div>
  <div class="col-xs-6">
    <%= image_tag(@entry.image_url, :class => 'img-responsive')  %>
  </div>
</div>
